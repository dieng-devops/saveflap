%h2.page-header= page_title

- Settings.grouped(2).each do |row|
  .row
    - row.each do |group_name, values|
      .col-lg-6.col-md-6
        .panel.panel-default
          .panel-heading
            %h4.panel-title
              = link_to icon(settings_icon_for(group_name), t("settings.#{group_name}")), "##{group_name}", data: { toggle: 'collapse' }
          .panel-collapse.collapse.in{ id: group_name }
            %table.table.table-bordered
              %tbody
                - values.each do |key, value|
                  %tr
                    %td= key
                    %td
                      %code= truncate value, length: 64
